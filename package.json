{
  "private": true,
  "scripts": {
    "clean": "del output .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "upgrade": "ncu -u"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "npm-check-updates": "^2.14.0",
    "pulp": "^12.0.1",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.6.0"
  }
}
